[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hurricane"
version = "0.1.0"
description = "Regression pipeline for hurricane position prediction"
authors = [ { name = "" } ]